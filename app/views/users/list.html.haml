%h1 Users List

%ul
  - @users.each do |user|
    %li
      = user.username
      %span &vert;
      = user.email
      %span &vert;
      %a(href="/users/${user.id}/edit") edit
      %span &vert;
      %a(href="/users/${user.id}/delete") delete
