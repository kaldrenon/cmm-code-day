%h1 Users List

%ul
  - @users.each do |user|
    %li
      = user.username
      %span &vert;
      = user.email
      %span &vert;
      %a(href="/users/#{user.id}/edit") edit
      %span &vert;
      = link_to 'delete', user_path(user), method: :delete
